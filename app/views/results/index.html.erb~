<p id="notice"><%= notice %></p>
<div class="container">
  <div class="table-responsive">
    <div class="panel panel-default">
      <div class="panel-heading">Wyniki</div>
        <table class="table table-bordered table-condensed table-striped">
	  <thead>
	    <tr>
	      <th>Komitet</th>
	      <th>Okręg</th>
	      <th>Głosy</th>
	      <th colspan="3"></th>
	    </tr>
	  </thead>
	  <tbody>
	  <% @results.each do |result| %>
	      <tr>
		<td><%= result.committee.name %></td>
		<td><%= result.constituency.name %></td>
		<td><%= result.amount %></td>
		<td><%= link_to 'Wyświetl', result %></td>
		<td><%= link_to 'Edytuj', edit_result_path(result) %></td>
		<td><%= link_to 'Usuń', result, method: :delete, data: { confirm: 'Jesteś pewien?' }, class:"alert alert-warning" %></td>
	      </tr>
	    <% end %>
	  </tbody>
	</table>
      </div>
    </div>
  </div>
  <div class= "intro-text text-center">
    <%= link_to 'New Constituency', new_result_path %>
  </div>
</div>


