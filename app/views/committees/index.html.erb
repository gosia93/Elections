<head>
  <p id="notice"><%= notice %></p>
  <div class="container">
    <div class="table-responsive">
      <div class="panel panel-default">
         <div class="panel-heading">Lista Komitetów Wyborczych</div>
           <table class="table table-bordered table-condensed table-striped">
             <thead>
               <tr>
                <th>Nazwa</th>
                <th colspan="3"></th>
              </tr>
            </thead>

            <tbody>
              <% @committees.each do |committee| %>
                <tr>
                 <td><%= committee.name %></td>
                 <td><%= link_to 'Wyświetl', committee %></td>
                 <td><%= link_to 'Edytuj', edit_committee_path(committee) %></td>
                 <td><%= link_to 'Usuń', committee, method: :delete, data: { confirm: 'Jesteś pewien?' } %></td>
                </tr>
              <% end %>
            </tbody>
         </table>
      </div>
    </div>
  </div>
  <%= link_to 'Dodaj nowy', new_committee_path %>
  </div>


